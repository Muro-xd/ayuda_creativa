---
import Layout from '../../layouts/Layout.astro';
import Sidebar from '../../components/Sidebar.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import WhatsAppWidget from '../../components/WhatsAppWidget.astro';

const title = "Kardex o Inventario";
const description = "";

const ventas = [
      "▪️¿Cómo Hacer Ingreso de Mercadería?",
      "▪️¿Cómo Agregar Salidas?"
];

function slugify(text: string) {
  return text
    .toLowerCase()
    .replace(/[^a-z0-9]/g, '-')
    .replace(/-+/g, '-')
    .replace(/^-|-$/g, '');
}

const currentPath = Astro.url.pathname;
---

<Layout title={title}>
    <Header />
  <div class="flex flex-col md:flex-row max-w-6xl mx-auto">
    <div class="md:w-3/4 p-4">
      <h1 class="text-3xl font-bold text-primary mb-4">{title}</h1>
      <p class="text-gray-600 mb-6">{description}</p>

      <ul class="space-y-4">
        {ventas.map((ventas) => {
          const slug = slugify(ventas);
          return (
            <li>
              <a
                href={`/kardex/${slug}`}
                class="block text-lg text-gray-800 hover:text-primary font-medium"
              >
                {ventas}
              </a>
            </li>
          );
        })}
      </ul>
    </div>
  </div>
  <Footer />
  <WhatsAppWidget /> 
</Layout>
